source("renv/activate.R")

# This helped, but commenting it to reproduce the initial behavior
# Sys.setenv(OMP_NUM_THREADS="1")
# Sys.setenv(OPENBLAS_NUM_THREADS="1")
# Sys.setenv(MKL_NUM_THREADS="1")
